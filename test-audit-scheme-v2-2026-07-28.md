# Аудит редизайна схемы склада-хаба (v2)
**Дата:** 2026-07-28  
**Коммит:** `4b9421f` — "Схема склада-хаба: редизайн по эскизу дизайнера"  
**Проверяющий:** Агент-тестировщик  

---

## ⚠️ CRITICAL: style.min.css не обновлён после редизайна

**Проблема:** `index.html` загружает `/css/style.min.css` (строка 206), но этот файл **не был перегенерирован** после коммита `4b9421f`.

**Что в style.min.css сейчас (устаревшее):**
- Старые классы `.cw-*` (`cw-flex`, `cw-top-row`, `cw-node`, `cw-hub`, `cw-bottom` и т.д.) — **24 селектора**
- Старый `aspect-ratio: 2/1` на `.consolidation-wh__diagram`
- Старые `.consolidation-wh__flow--*` с `position: absolute` для узлов
- Старый `.consolidation-wh__arrows { position: absolute; inset: 0; ... }` (вместо `display: block; position: relative; height: 36px`)
- Старый `.consolidation-wh__hub` без `linear-gradient`, без `gold border: 2px`, без `animation: hubGlowPulse`
- Нет `.consolidation-wh__hub:hover`
- Нет `.consolidation-wh__node:hover .consolidation-wh__node-icon`
- Нет `.consolidation-wh__bottom:hover`
- Нет `.consolidation-wh__economy::before` (золотая левая полоса)
- Нет `@keyframes hubGlowPulse`
- Нет `.consolidation-wh__top-row`, `__gap`, `__gap-line`, `__gap-arrow`, `__down-arrow`, `__bottom-icon`, `__bottom-label`
- Мобильная адаптация (≤480px) — **неполная**, отсутствуют новые медиа-правила

**Статистика:**
| Файл | Дата (timestamp) | Размер |
|------|------------------|--------|
| `css/style.css` | 14:45 (1785239159) | 42,567 байт |
| `css/style.min.css` | 14:29 (1785238186) | 30,149 байт |

Разница ~16 минут — min-файл **не включил** ~461 строку новых стилей.

**Что нужно сделать:**
```bash
cd /home/openclaw/.openclaw/workspace/logistics-site
cat css/style.css | npx csso --output css/style.min.css
```

---

## ✅ ПРОВЕРЕНО — Всё в порядке

### 1. HTML (`index.html`) — секция #consolidation-wh
- ✅ Секция существует, ID `consolidation-wh` (строка 588)
- ✅ Заголовок: "Консолидированный склад <span class='gold'>в Казахстане</span>"
- ✅ Подзаголовок: "Одна партия — одна доставка... до 30%"
- ✅ Теглайн: `<span class="section__tagline">Экономия</span>`
- ✅ Три узла поставщиков с разными лейблами (Поставщик 1, 2, 3)
- ✅ Иконки: SVG грузовик `stroke="#C9A96E"` 20×20
- ✅ Разделители между узлами (`.consolidation-wh__gap`)
- ✅ Y-образное слияние: inline SVG `viewBox="0 0 320 40"`
- ✅ Хаб (`.consolidation-wh__hub`) с glow, иконкой склада 44×44, "Склад-хаб" / "Казахстан / Киргизия"
- ✅ Стрелка вниз `.consolidation-wh__down-arrow` от хаба
- ✅ Нижний блок `.consolidation-wh__bottom` — "Одна партия → в РФ"
- ✅ Текстовый блок справа: абзац, буллеты (3 шт), блок экономии (до 30%), CTA "Рассчитать выгоду"
- ✅ Все тексты соответствуют `kz-consolidation-texts.json`

### 2. CSS (`css/style.css`) — основной файл
- ✅ Новые стили `.consolidation-wh__*` присутствуют (строки 1666–2030+)
- ✅ Старые классы `.cw-*` **отсутствуют** — полностью удалены
- ✅ Цвета — только CSS-переменные (--gold, --primary, --bg-card, --border, --text-light, --radius)
- ✅ Анимация `@keyframes hubGlowPulse` (3s ease-in-out infinite)
- ✅ Ховеры: узел (icon), хаб, нижний блок, буллеты
- ✅ Мобильная адаптация ≤768px (grid → 1 колонка, уменьшенные размеры)
- ✅ Мобильная адаптация ≤480px (ещё более компактные размеры)
- ✅ Баланс скобок: 348 открывающих `{` и 348 закрывающих `}` — совпадает

### 3. Функциональность
- ✅ Ссылка меню "Склад в Казахстане" (строка 273) ведёт на `href="#consolidation-wh"`
- ✅ Секция расположена корректно: после `#transit-kz` (строка 441), перед `#route` (строка 755) и `#summary` (строка 887)
- ✅ HTML-теги сбалансированы (проверено парсером — все открывающие/закрывающие совпадают)
- ✅ DOCTYPE `<!DOCTYPE html>` присутствует

### 4. Дополнительно
- ✅ `kz-consolidation-design.html` — старый дизайн-файл, не обновлялся (не является частью production)
- ✅ `index_new_scheme.html` — старый тестовый файл с `cw-*`, не используется
- ✅ Другие секции сайта не затронуты — `<section>` блоки не изменились
- ✅ CHANGES.md обновлён (запись #6 описывает редизайн)

### 5. Сравнение с дизайн-спецификацией (`scheme_design.md`)
- ✅ Структура секции соответствует спецификации (п. 2)
- ✅ Flexbox-схема без `position: absolute` для узлов (п. 3)
- ✅ SVG-стрелки слияния viewBox="0 0 320 40" (п. 6 и разд. 6)
- ✅ Размерная сетка совпадает (п. 11)
- ✅ Типографика совпадает (п. 12)
- ✅ Тексты совпадают (п. 13)
- ✅ Подсветка/цветовая схема совпадает (п. 14)

---

## ИТОГО: 1 CRITICAL баг

| # | Приоритет | Проблема | Файл | Исправление |
|---|-----------|----------|------|-------------|
| 1 | **CRITICAL** | `style.min.css` устарел: содержит старые `cw-*` стили, не содержит новых `.consolidation-wh__*` классов | `css/style.min.css` | `cat css/style.css \| npx csso --output css/style.min.css` |

**Примечание:** После регенерации min-файла — схема будет отображаться корректно. Без этого — сайт загружает устаревшие CSS-стили и схема не отрисовывается по дизайну.

В целом HTML и CSS (основной файл) выполнены качественно, все пункты спецификации соблюдены. Редизайн готов к деплою после регенерации min-файла.
